{
  "name": "eventfulserver",
  "version": "1.0.0",
  "description": "EventFull",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently --kill-others-on-fail \"node server.js\" \"cd client && yarn start\"",
    "client": "cd client && node start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "concurrently": "^4.1.0",
    "express": "^4.16.3",
    "react-scripts": "^2.1.5"
  }
}

